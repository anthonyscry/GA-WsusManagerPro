---
phase: 27-visual-feedback-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/WsusManager.App/Views/MainWindow.xaml
autonomous: true
requirements:
  - UX-10

must_haves:
  truths:
    - "All toolbar buttons have tooltip help text on hover"
    - "All quick action buttons have tooltip help text on hover"
    - "Navigation tabs without obvious labels have tooltips"
    - "Tooltip text is short (5-10 words) describing button function"
  artifacts:
    - path: "src/WsusManager.App/Views/MainWindow.xaml"
      provides: "Tooltip attributes on all buttons"
      contains: "ToolTip="
  key_links:
    - from: "Button elements"
      to: "ToolTip service"
      via: "ToolTip attribute"
      pattern: "ToolTip=\"[^\"]+\""
---

<objective>
Add tooltip help text to all toolbar and quick action buttons so users understand button functions without clicking.

Purpose: Tooltips provide contextual help for new users and confirm button functions for experienced users. Short descriptions prevent accidental clicks and improve discoverability.

Output: All buttons show 5-10 word tooltip on hover describing their function.
</objective>

<execution_context>
@/home/anthonyscry/.claude/get-shit-done/workflows/execute-plan.md
@/home/anthonyscry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-visual-feedback-polish/27-CONTEXT.md
@src/WsusManager.App/Views/MainWindow.xaml
</context>

<tasks>

<task type="auto">
  <name>Add tooltips to sidebar navigation buttons</name>
  <files>src/WsusManager.App/Views/MainWindow.xaml</files>
  <action>
    Add ToolTip attributes to all sidebar navigation buttons (around lines 46-161):

    SETUP Category:
    - Install WSUS button: `ToolTip="Install WSUS with SQL Server Express"`
    - Create GPO button: `ToolTip="Copy GPO templates for domain deployment"`

    TRANSFER Category:
    - Export / Import button: `ToolTip="Export metadata to USB or import from external media"`

    MAINTENANCE Category:
    - Online Sync button: `ToolTip="Synchronize with Microsoft Update servers"`
    - Schedule Task button: `ToolTip="Create scheduled task for automated operations"`

    DIAGNOSTICS Category:
    - Diagnostics button: `ToolTip="Run health checks and auto-repair"`

    CLIENT TOOLS Category:
    - Client Tools button: `ToolTip="Remote client management and diagnostics"`

    DATABASE Category:
    - Database button: `ToolTip="Backup, restore, and cleanup operations"`

    Bottom utility buttons:
    - Dashboard button: `ToolTip="View dashboard health status"` (optional, already obvious from label)
    - Settings button: `ToolTip="Configure application settings"` (optional, already obvious)
    - Help button: `ToolTip="View keyboard shortcuts and help"` (optional, already obvious)
    - About button: `ToolTip="Version and author information"` (optional, already obvious)

    DO NOT: Add tooltips to buttons with obvious labels (Dashboard, Settings, Help, About) — per CONTEXT.md, focus on buttons without obvious labels
  </action>
  <verify>
    dotnet build src/WsusManager.App/WsusManager.App.csproj succeeds, sidebar buttons have ToolTip attributes
  </verify>
  <done>
    All sidebar navigation buttons have ToolTip attributes with 5-10 word descriptions
  </done>
</task>

<task type="auto">
  <name>Add tooltips to Quick Actions buttons</name>
  <files>src/WsusManager.App/Views/MainWindow.xaml</files>
  <action>
    Add ToolTip attributes to Quick Action buttons (around lines 297-320):

    Find each Quick Action button and add tooltip:
    - Diagnostics: `ToolTip="Run health checks and auto-repair"`
    - Deep Cleanup: `ToolTip="Remove obsolete updates and optimize database"`
    - Online Sync: `ToolTip="Synchronize with Microsoft Update servers"`
    - Start Services: `ToolTip="Start SQL, WSUS, and IIS services"`
    - Toggle Mode: Already has tooltip — keep existing

    DO NOT: Add tooltips for buttons where function is obvious from label (Start Services is borderline but include for consistency)
  </action>
  <verify>
    dotnet build src/WsusManager.App/WsusManager.App.csproj succeeds, Quick Action buttons have ToolTip attributes
  </verify>
  <done>
    All Quick Action buttons have ToolTip attributes with short descriptions
  </done>
</task>

<task type="auto">
  <name>Add tooltips to toolbar refresh and settings buttons in header</name>
  <files>src/WsusManager.App/Views/MainWindow.xaml</files>
  <action>
    Check for toolbar buttons in header area (around lines 180-207) and add tooltips if missing:

    1. Look for refresh or settings icon buttons in header
    2. If found, add ToolTip attributes:
       - Refresh button: `ToolTip="Refresh dashboard data"`
       - Settings button: `ToolTip="Open settings dialog"` (if separate from sidebar)

    3. If no toolbar buttons exist in header (only status display), this task is complete

    DO NOT: Add tooltips to status text elements (ConnectionStatusText, ModeOverrideIndicator) — these are not interactive
  </action>
  <verify>
    dotnet build src/WsusManager.App/WsusManager.App.csproj succeeds, header toolbar buttons have ToolTip attributes if present
  </verify>
  <done>
    All interactive buttons in header toolbar have ToolTip attributes
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: dotnet build src/WsusManager.App
2. All sidebar navigation buttons have ToolTip attributes
3. All Quick Action buttons have ToolTip attributes
4. Header toolbar buttons have ToolTip attributes (if present)
5. Tooltip text is 5-10 words describing button function
6. Tooltips appear on hover in running application
</verification>

<success_criteria>
All toolbar and quick action buttons display tooltip help text on hover. Tooltips provide short, clear descriptions of button functions, improving discoverability and preventing accidental clicks.
</success_criteria>

<output>
After completion, create `.planning/phases/27-visual-feedback-polish/27-05-SUMMARY.md`
</output>
