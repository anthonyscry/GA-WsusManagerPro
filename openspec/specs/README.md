# OpenSpec Specs

This directory tracks stable product intent for WSUS Manager.

- `openspec/changes/` holds proposed and in-flight changes.
- Each change includes `proposal.md` (what/why) and `tasks.md` (execution plan).
- Promote accepted behavior from changes into durable specs here.
