# OpenSpec Archive

This directory stores completed OpenSpec change bundles.

- `openspec/changes/` is for proposed or in-flight work.
- Move a change folder here once acceptance criteria are met and implementation is shipped.
